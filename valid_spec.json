 @Query("SELECT " +
            "    new com.fxlabs.fxt.dao.entity.project.job " +
            "FROM " +
            "    FROM job as j JOIN job_issue_tracker as jit on j.job_issue_tracker_id = jit.id " +
            "WHERE " +
            "  j.project_id = ?1" +
            "And j.inactive = false And jit.account_type = 'No_Issues'")
			
			
			SELECT *
    FROM job as j JOIN job_issue_tracker as jit on j.job_issue_tracker_id = jit.id where j.project_id = '40288355687a482b01687ad1ee700000'
	And j.inactive = false And jit.account_type = 'No_Issues' FETCH NEXT 0 ROWS ONLY
	
  @Query("SELECT " +
            " j.issueTracker.id " +
            "FROM " +
            "    Job j " +
            "WHERE " +
            "    j.project.id = ?1 AND j.inactive = false")
   List<String> IssueListIds = this.jobRepository.findByProjectId(projectId);
       // Page<JobIssueTracker> page = this.jobIssueTrackerRepository.findByIdInAndAccountType(IssueListIds, AccountType.No_Issues, pageable);
     //   List<com.fxlabs.fxt.dto.project.JobIssueTracker> issuesList = jobIssueTrackerConverter.convertToDtos(page.getContent());
//        List<String> IdsList = new ArrayList<>();
//
//        issuesList.forEach(ids ->{
//
//            IdsList.add(ids.getId());
//
//        });

//        List<Job> newList = this.jobRepository.findByIssueTrackerIdInAndInactive(IdsList, false);